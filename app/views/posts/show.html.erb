<div class="form-post">
  <h1><%= @post.title %></h1>
  <p><%= @post.content %></p>
  <p>
  <% @post.tags.each do |tag| %>
    <span class="tag <%= tag.style %>"><%= tag.name %></span>
  <% end %>
  </p>
</div>